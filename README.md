# 🔍 PDF Search with OCR

![Python Version](https://img.shields.io/badge/python-3.8+-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Status](https://img.shields.io/badge/status-active-brightgreen.svg)

## 📋 О проекте

Интеллектуальный поисковик по отсканированным PDF-файлам журналов с использованием технологии OCR (Optical Character Recognition). Программа позволяет искать текст даже в сканированных документах, где текст представлен в виде изображений.

### ✨ Основные возможности

- 🔍 **Полнотекстовый поиск** по отсканированным PDF-документам
- 🏷️ **Распознавание текста** с поддержкой русского и английского языков
- ⚡ **Кэширование результатов** для ускорения повторных поисков
- 📊 **Контекстный вывод** найденных совпадений
- 🖼️ **Автоматическая предобработка** изображений для улучшения качества распознавания
- 💾 **Сохранение результатов** в текстовые файлы
- 🎯 **Гибкие параметры поиска**: целые слова/подстроки, учет регистра

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.8 или выше
- Windows 10/11 (также работает на Linux/macOS с небольшими изменениями)

### Шаг 1: Клонирование и настройка

```bash
# Клонируйте репозиторий
git clone https://github.com/ByP0/pdf-search.git
cd pdf-search

# Создайте виртуальное окружение
python -m venv .venv

# Активируйте виртуальное окружение
# Для Windows:
.venv\Scripts\activate
# Для Linux/macOS:
source .venv/bin/activate
```

### Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 3: Установка Tesseract OCR

#### Для Windows:
1. 📥 **Скачайте установщик Tesseract 5.5.0**:  
   [tesseract-ocr-w64-setup-5.5.0.20241111.exe](https://github.com/UB-Mannheim/tesseract/releases/download/5.5.0/tesseract-ocr-w64-setup-5.5.0.20241111.exe)

2. 🛠️ **Установите Tesseract**:
   - Запустите установщик
   - Выберите папку установки (по умолчанию: `C:\Program Files\Tesseract-OCR`)
   - **Важно:** Выберите русский и английский языки в компонентах
   - Завершите установку

3. 🔧 **Добавьте в системный PATH**:
   ```
   Win + R → sysdm.cpl → "Дополнительно" → "Переменные среды"
   → "Системные переменные" → "Path" → "Изменить"
   → "Создать" → Добавьте: C:\Program Files\Tesseract-OCR
   → Нажмите "OK" во всех окнах
   ```

4. ✅ **Проверьте установку**:
   ```bash
   tesseract --version
   ```

#### Для Ubuntu/Debian:
```bash
sudo apt-get update
sudo apt-get install tesseract-ocr
sudo apt-get install tesseract-ocr-rus
```

#### Для macOS:
```bash
brew install tesseract
brew install tesseract-lang
```

### Шаг 4: Установка Poppler 

1. 📥 **Скачайте Poppler 24.08.0**:  
   [Release-24.08.0-0.zip](https://github.com/oschwartz10612/poppler-windows/releases/download/v24.08.0-0/Release-24.08.0-0.zip)

2. 📂 **Распакуйте архив** в удобное место

3. 🔧 **Добавьте путь к `bin` в PATH**:
   ```
   Путь будет выглядеть примерно так:
   D:\Programs\poppler\Library\bin
   ```

### Шаг 5: Настройка структуры проекта

```bash
# Создайте необходимые папки
mkdir local        # для PDF-файлов
mkdir ocr_cache    # для кэша распознанного текста

# Поместите ваши PDF-файлы в папку 'local'
```

📁 **Структура проекта:**
```
pdf-search/
├── search_journal.py    # основной скрипт
├── requirements.txt     # зависимости Python
├── local/              # PDF-файлы для поиска
├── ocr_cache/          # кэш распознанного текста
└── README.md           # документация
```

## 🎮 Использование

### Режим 1: Интерактивный поиск

```bash
python search_journal.py
```

Пример сессии:
```
============================================================
🔍 ПОИСК ПО ЖУРНАЛАМ (OCR)
============================================================

Введите слова для поиска (через пробел) или 'q' для выхода:
> археология находка

Параметры поиска:
1. Учитывать регистр
2. Искать частичные совпадения
3. Обычный поиск (целые слова, без учета регистра)
Выберите вариант (1-3, по умолчанию 3): 3

🔎 Начинаю поиск: археология, находка...
✓ Найдено 2 PDF файлов
...
```

### Режим 2: Командная строка

```bash
# Простой поиск
python search_journal.py археология

# Поиск нескольких слов
python search_journal.py археология находка раскопки

# С учетом регистра
python search_journal.py "Археология" --case-sensitive

# Частичные совпадения
python search_journal.py арх --partial

# Указать другую папку с PDF
python search_journal.py наука --folder "мои_журналы"

# Отключить кэширование
python search_journal.py история --no-cache

# Использовать больше потоков для обработки
python search_journal.py поиск --threads 4
```

### Режим 3: Использование как модуля

```python
from search_journal import JournalSearchEngine
from pathlib import Path

# Создаем поисковый движок
engine = JournalSearchEngine(
    pdfs_folder=Path('local'),
    cache_folder=Path('ocr_cache')
)

# Выполняем поиск
results = engine.search_across_all_pdfs(
    search_words=['археология', 'история'],
    case_sensitive=False,
    match_whole_word=True
)

# Обрабатываем результаты
for result in results:
    print(f"Файл: {result['filename']}")
    print(f"Совпадений: {result['total_matches']}")
    for detail in result['details']:
        print(f"  Страница {detail['page']}:")
        for match in detail['matches']:
            print(f"    - {match['word']}: {match['context']}")
```

## ⚙️ Параметры командной строки

| Параметр | Описание | Пример |
|----------|----------|---------|
| `words` | Слова для поиска (обязательный) | `археология история` |
| `--folder` | Папка с PDF-файлами | `--folder "журналы"` |
| `--cache` | Папка для кэша | `--cache "мой_кеш"` |
| `--no-cache` | Не использовать кэш | `--no-cache` |
| `--case-sensitive` | Учитывать регистр | `--case-sensitive` |
| `--partial` | Искать частичные совпадения | `--partial` |
| `--threads` | Количество потоков | `--threads 4` |

## 🔧 Технические детали

### 📊 Производительность

| Операция | Время (приблизительно) |
|----------|------------------------|
| Первая обработка PDF (100 страниц) | 2-5 минут |
| Повторный поиск (с кэшем) | 1-10 секунд |
| Обработка 1 страницы | 2-5 секунд |

### 🗄️ Кэширование

- Распознанный текст сохраняется в папке `ocr_cache/`
- Кэш ускоряет повторные поиски в 10-100 раз
- Для очистки кэша удалите папку `ocr_cache/`

### 🌐 Поддерживаемые форматы

- PDF (все версии)
- Отсканированные документы
- Фотографии текста (через конвертацию в PDF)

### 🔤 Поддерживаемые языки

- Русский (основной)
- Английский
- Можно добавить другие языки через Tesseract

## 🐛 Устранение неполадок

### Проблема: "Tesseract не найден"
```
❌ TESSERACT OCR НЕ НАЙДЕН!
```
**Решение:**
1. Проверьте установку Tesseract: `tesseract --version`
2. Убедитесь, что путь добавлен в PATH
3. Перезапустите терминал


### Проблема: Медленная обработка
**Решение:**
1. Уменьшите DPI в настройках (строка 42 в коде): `self.dpi = 150`
2. Используйте кэширование
3. Обрабатывайте меньше страниц за раз

### Проблема: Плохое качество распознавания
**Решение:**
1. Увеличьте DPI: `self.dpi = 300`
2. Отрегулируйте параметры предобработки в функции `preprocess_image()`
3. Используйте оригинальные PDF с более высоким качеством сканирования

## 📈 Оптимизация

### Для больших архивов
```python
# В классе JournalSearchEngine измените:
self.dpi = 150  # вместо 200
self.max_workers = 1  # для стабильности
```

### Для максимальной точности
```python
self.dpi = 300
self.ocr_languages = "rus+eng+fra+deu"  # дополнительные языки
```

## 🙏 Благодарности

- [Tesseract OCR](https://github.com/tesseract-ocr/tesseract) - за отличную OCR библиотеку
- [PyMuPDF](https://github.com/pymupdf/PyMuPDF) - за работу с PDF